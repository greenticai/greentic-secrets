{
  "secret_requirements": [
    {
      "id": "aws_access_key_id",
      "type": "string",
      "description": "AWS Access Key ID for the integration (omit if using web identity/role).",
      "required": false,
      "env": "AWS_ACCESS_KEY_ID",
      "redact": true
    },
    {
      "id": "aws_secret_access_key",
      "type": "string",
      "description": "AWS Secret Access Key for the integration (omit if using web identity/role).",
      "required": false,
      "env": "AWS_SECRET_ACCESS_KEY",
      "redact": true
    },
    {
      "id": "aws_web_identity_token_file",
      "type": "string",
      "description": "Path to web identity token for IRSA/oidc auth (optional).",
      "required": false,
      "env": "AWS_WEB_IDENTITY_TOKEN_FILE",
      "redact": true
    },
    {
      "id": "audit_sink_credentials",
      "type": "string",
      "description": "Credentials or token for credentialed audit sinks (Splunk/GCP/Azure/http). Required when audit.sink_type is splunk, azure, gcp, or http.",
      "required": false,
      "redact": true
    }
  ]
}
